# Notes

- Keep teaching practical and engineer-oriented.
- Start with TLS and certificate trust before moving to application auth.
- Separate transport security from authorization every time.
- Use OpenSSL demos early so the abstractions stay grounded.
- When the user says "network security," the next best lesson is usually TLS handshake and certificate chain validation.
