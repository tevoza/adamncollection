# Claude Code

Agent instructions for this repo live in [`AGENTS.md`](AGENTS.md) — read that first.

There's no repo-local `.claude/` configuration (skills, hooks, subagents) yet. If that changes, document it here.
